<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!--
        Đơn vị persistence dùng cho toàn bộ project (User, Category, Product).
        Đổi jakarta.persistence.jdbc.url / user / password bên dưới cho khớp
        với SQL Server đang chạy trên máy của bạn. Chỉ cần tạo sẵn database
        rỗng "CREATE DATABASE ServletCRUDMVC;" - Hibernate sẽ tự tạo/cập nhật
        bảng nhờ hibernate.hbm2ddl.auto = update, không cần chạy tay create_tables.sql.
    -->
    <persistence-unit name="ShoppingPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>vn.iotstar.model.User</class>
        <class>vn.iotstar.model.Category</class>
        <class>vn.iotstar.model.Product</class>

        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <property name="jakarta.persistence.jdbc.driver" value="com.microsoft.sqlserver.jdbc.SQLServerDriver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:sqlserver://localhost:1433;databaseName=ServletCRUDMVC;encrypt=false;trustServerCertificate=true"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value="1452"/>

            <property name="hibernate.dialect" value="org.hibernate.dialect.SQLServerDialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
        </properties>
    </persistence-unit>
</persistence>
